/* * Тестируем rnd функции */ describe('rnd', function () {	var rnd = require('../lib/rnd.js');		it('should have #create', function () {		rnd.create.should.be.a('function');	});		it('should have #rndStr', function () {		rnd.create.should.be.a('function');	});		it('should have #rndInt', function () {		rnd.create.should.be.a('function');	});		describe('#rndStr', function () {		it('should return random string length === 10', function () {			var str = rnd.rndStr();			str.length.should.eql(10);			str.should.match(/^\w+$/i);		});				it('should return random string length === 32', function () {			var str = rnd.rndStr(32);			str.length.should.eql(32);			str.should.match(/^\w+$/i);		});	});		describe('#rndInt', function () {		it('should return random string length === 10', function () {			var str = rnd.rndInt();			str.length.should.eql(10);			str.should.match(/^\w+$/i);		});				it('should return random string length === 32', function () {			var str = rnd.rndInt(32);			str.length.should.eql(32);			str.should.match(/^\d+$/);		});	});});